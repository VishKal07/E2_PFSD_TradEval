<<<<<<< HEAD
version: '3.8'

services:
  mongodb:
    image: mongo:6.0
    container_name: tradeval_mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: password123
      MONGO_INITDB_DATABASE: tradeval
    volumes:
      - mongodb_data:/data/db

  redis:
    image: redis:7-alpine
    container_name: tradeval_redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  backend:
    build: .
    container_name: tradeval_backend
    restart: always
    ports:
      - "8000:8000"
    depends_on:
      - mongodb
      - redis
    environment:
      MONGODB_URI: mongodb://admin:password123@mongodb:27017/
      REDIS_URL: redis://redis:6379
      DEBUG: "True"
    volumes:
      - ./ml/model:/app/ml/model
      - ./frontend:/app/frontend
    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             daphne -b 0.0.0.0 -p 8000 tradeeval_backend.asgi:application"

volumes:
  mongodb_data:
  redis_data:
=======
version: "3.9"

services:

  # ── MongoDB ───────────────────────────────────────────────────
  mongo:
    image: mongo:6.0
    container_name: tradeval_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db        # data persists across restarts
    environment:
      MONGO_INITDB_DATABASE: tradeval
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── Django Backend ────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: tradeval_backend
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app             # live reload during development
      - ./ml:/ml                   # access to ML models
      - ./Data:/Data               # access to CSV data
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=tradeeval_backend.settings
      - MONGODB_URI=mongodb://mongo:27017/tradeval
    depends_on:
      mongo:
        condition: service_healthy  # wait for mongo to be ready

  # ── Frontend ──────────────────────────────────────────────────
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: tradeval_frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    depends_on:
      - backend

volumes:
  mongo_data:                      # named volume so data survives restarts
>>>>>>> 2a09392ae0b99a3752c7c750780483f44f55dcce
